"""Forms for the Mosaico app.

Currently relies on Django's built-in ``UserCreationForm`` for registration.
Custom forms can be added here as the project grows.
"""

from django.contrib.auth.forms import UserCreationForm  # noqa: F401
